const botton = document.getElementById('resultado');

function resultado() {
  var concat = number;
  concat = concat.sort(
    function(a, b) { return a - b });

  console.log(concat);

  var length = concat.length;

  if (length % 2 == 1) {

    console.log(concat[(length / 2) = resultado])

    return concat[(length / 2)]
 }
 resultado.textContent = `${nome} seu IMC é ${valorIMC} e você está ${classificacao}`;
}

